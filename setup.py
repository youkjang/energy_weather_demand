"""Compatibility setup.py for editable installs.

Project metadata and dependencies are defined in pyproject.toml.
This file is included mainly for tools or workflows that still expect setup.py.
"""

from setuptools import setup

setup()
